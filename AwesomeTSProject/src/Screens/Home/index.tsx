import React from 'react';
import { Text, View } from "react-native";

export const Home = (props: any) => {
    return (
        <View style={{flex: 1}}>
            <Text>ddd</Text>
        </View>
    )
}